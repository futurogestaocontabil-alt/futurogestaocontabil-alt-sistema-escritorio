import {useEffect,useRef,type PropsWithChildren,type ReactNode} from 'react';import {Inbox,X} from 'lucide-react';export {default as Button} from './Button';
export function Card({children,className=''}:PropsWithChildren<{className?:string}>){return <section className={'card '+className}>{children}</section>;}
export function Badge({children,tone='neutral'}:PropsWithChildren<{tone?:string}>){return <span className={'badge badge-'+tone}>{children}</span>;}
export function PageHeader({eyebrow,title,description,children}:PropsWithChildren<{eyebrow?:string;title:string;description?:string}>){return <div className="page-header"><div>{eyebrow&&<span className="eyebrow">{eyebrow}</span>}<h1>{title}</h1>{description&&<p>{description}</p>}</div><div className="header-actions">{children}</div></div>;}
export function EmptyState({title,description,children}:PropsWithChildren<{title:string;description:string}>){return <div className="empty-state"><span className="empty-icon"><Inbox size={25}/></span><h3>{title}</h3><p>{description}</p>{children}</div>;}
export function Field({label,children,hint}:PropsWithChildren<{label:string;hint?:string}>){return <label className="field"><span>{label}</span>{children}{hint&&<small>{hint}</small>}</label>;}
export function StatCard({label,value,detail,icon,onClick}: {label:string;value:ReactNode;detail?:string;icon?:ReactNode;onClick?:()=>void}){return <button className="stat-card" onClick={onClick} type="button" disabled={!onClick}><div className="stat-label">{label}{icon}</div><strong>{value}</strong>{detail&&<small>{detail}</small>}</button>;}
export function Modal({title,children,onClose,wide=false}:PropsWithChildren<{title:string;onClose:()=>void;wide?:boolean}>){const ref=useRef<HTMLDialogElement>(null);useEffect(()=>{const dialog=ref.current;dialog?.showModal();return ()=>dialog?.close();},[]);return <dialog ref={ref} className={'modal '+(wide?'modal-wide':'')} onCancel={onClose} onClick={e=>{if(e.target===ref.current)onClose();}}><div className="modal-header"><h2>{title}</h2><button className="button button-ghost button-icon" aria-label="Fechar janela" onClick={onClose}><X size={20}/></button></div>{children}</dialog>;}
